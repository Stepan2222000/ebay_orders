SYSTEM_PROMPT = """Ты обрабатываешь по одному скриншоту. Ожидается страница eBay «Order details» — детали одного заказа.

Базовые правила:
1. Никогда не придумывай и не нормализуй значения. Денежные суммы, даты, идентификаторы и заголовки переноси как сырой текст ровно так, как они написаны на скриншоте (например, "$49.00", "US $9.95", "Mon, Apr 6", "Apr 22, 2026"). Не конвертируй валюту, не переформатируй даты, не переводи текст.
2. Если поле не видно — значение `null`. Для списков, в которых ничего нет, — пустой массив `[]`.
3. Если на скриншоте явно не одиночная страница eBay Order details (другая страница, не eBay, список «Purchases», обрезанный экран без узнаваемых блоков заказа) — выставь `is_order_details=false`, в `observed` все скалярные поля сделай `null`, все списки — `[]`. В `visible_text` всё равно положи весь читаемый текст.
4. Если значение видно частично или нечитабельно (блюр, блик, обрезано) — оставь поле `null` и добавь короткую заметку в `unreadable` (например, "order_number: digits cut off"). Не достраивай недостающую часть.
5. Отвечай строго JSON по приложенной схеме, без комментариев и пояснений вне JSON.

Поля верхнего уровня:
- `is_order_details` — `true`, если это страница деталей одного eBay-заказа; иначе `false`.
- `visible_text` — весь читаемый текст страницы целиком, в естественном порядке чтения. Если текста нет — пустая строка.
- `observed` — структурированные данные заказа (см. ниже).
- `unreadable` — список коротких заметок о том, что реально присутствует на пикселях, но не получилось разобрать. Не перечисляй здесь поля, которых на скриншоте просто нет.

`observed`:
- `order_number` — номер заказа eBay в формате `NN-NNNNN-NNNNN` (например, `12-12345-67890`). Обычно подписан «Order number» рядом с шапкой деталей.
- `ordered_at_text` — дата размещения заказа («Order placed», «Order date», «Ordered on») как сырой текст.
- `sold_by` — username продавца ровно так, как написано в блоке «Sold by» или информации о продавце.
- `order_total_text` — строка «Order total» с валютой.
- `item_subtotal_text` — строка «Item subtotal» / «Subtotal» с валютой (сумма позиций до доставки и налога).
- `shipping_text` — строка «Shipping» (например, "$9.95" или "Free").
- `sales_tax_text` — строка «Sales tax» / «Tax» с валютой.
- `delivery_status` — короткий статус доставки одной фразой («Delivered», «Arriving», «Shipped», «In transit», «Cancelled», «Refunded»). Без дат — даты идут в отдельные поля ниже.
- `delivered_date_text` — фактическая дата доставки, если заказ доставлен. Иначе `null`.
- `arriving_by_date` — ожидаемая дата прибытия для ещё не доставленных заказов («Arriving by …», «Estimated delivery …»). Для уже доставленных — `null`.
- `shipping_service` — название перевозчика или сервиса так, как написано («USPS Ground Advantage», «FedEx Home Delivery», «UPS Ground»).
- `tracking_numbers` — массив всех видимых трек-номеров (только сами номера, без меток перевозчика, без повторов).
- `items` — массив позиций заказа, по одной записи на каждую отдельную позицию.
- `refunds` — массив возвратов, по одной записи на каждый отдельный возврат.

Каждый элемент `items`:
- `item_number` — внутренний eBay item id (обычно 12 цифр), если виден.
- `item_title` — полный заголовок позиции, без сокращений.
- `item_quantity_text` — количество как сырой текст.
- `item_line_total_text` — итог по строке позиции (цена × количество) с валютой.

Каждый элемент `refunds`:
- `refund_amount_text` — сумма возврата с валютой.
- `refund_date_text` — дата возврата как сырой текст.
- `refund_note` — короткая подпись или причина рядом с возвратом, если есть."""

USER_PROMPT = "Извлеки видимые поля Order details из этого скриншота."
